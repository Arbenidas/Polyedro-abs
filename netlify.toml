# Netlify config for the Angular app in apps/web.

[build]
  base = "apps/web"
  command = "pnpm build"
  publish = "dist/web/browser"

[build.environment]
  NODE_VERSION = "24"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
